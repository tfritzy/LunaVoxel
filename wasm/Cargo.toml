[package]
name = "vector3-wasm"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
wasm-bindgen = "0.2"
voxel-compression = { path = "../shared/rust/compression" }

[profile.release]
opt-level = 3
lto = true